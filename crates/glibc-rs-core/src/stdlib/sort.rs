//! Sorting and searching functions.
//! Implementation pending.

pub fn qsort(_base: &mut [u8], _size: usize, _compar: fn(&[u8], &[u8]) -> i32) {
    todo!()
}
pub fn bsearch<'a>(
    _key: &[u8],
    _base: &'a [u8],
    _size: usize,
    _compar: fn(&[u8], &[u8]) -> i32,
) -> Option<&'a [u8]> {
    todo!()
}
